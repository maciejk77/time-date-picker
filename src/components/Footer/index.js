import React from 'react';
import moment from 'moment';

const Footer = () => {
  return (
    <>
      <div />
    </>
  );
};

export default Footer;

// console.log(moment.localeData().ordinal(day));
